
add_subdirectory(event)
add_subdirectory(http)
add_subdirectory(mail)
add_subdirectory(misc)
add_subdirectory(os/unix)
add_subdirectory(core)
#add_executable(nginx ${SOURCE_FILES})
#target_link_libraries(nginx core )

